<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Login</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
<style>
    body {
        background: radial-gradient(circle at 10% 20%, rgba(99, 179, 237, 0.18), transparent 25%),
                    radial-gradient(circle at 80% 0%, rgba(129, 140, 248, 0.18), transparent 25%),
                    #f8fafc;
    }
    .auth-card {
        max-width: 900px;
        border: none;
        overflow: hidden;
        box-shadow: 0 20px 50px rgba(15, 23, 42, 0.12);
    }
    .hero-side {
        background: linear-gradient(135deg, #2563eb, #7c3aed);
    }
    .hero-side h2, .hero-side p {
        color: #f8fafc;
    }
</style>
</head>

<body class="d-flex align-items-center justify-content-center min-vh-100">
    <div class="card auth-card w-100">
        <div class="row g-0">
            <div class="col-lg-5 p-5 hero-side d-flex flex-column justify-content-between">
                <div>
                    <span class="badge bg-white text-primary mb-3">Welcome back</span>
                    <h2 class="fw-bold">Log in to continue</h2>
                    <p class="mt-3">Access your dashboard and keep writing great stories.</p>
                </div>
                <div class="text-white-50 small">
                    <p class="mb-1">New here?</p>
                    <a href="/signUp" class="link-light fw-semibold">Create an account</a>
                </div>
            </div>
            <div class="col-lg-7 p-5 bg-white">
                <div class="d-flex justify-content-between align-items-start mb-4">
                    <div>
                        <p class="text-muted mb-1 text-uppercase small">Login</p>
                        <h4 class="fw-bold mb-0">Welcome back</h4>
                    </div>
                    <a href="/" class="text-decoration-none text-muted">Home</a>
                </div>

                <% if (typeof error !== 'undefined') { %>
                    <div class="alert alert-danger mb-3"><%= error %></div>
                <% } %>

                <form action="/login" method="post" class="row g-3">
                    <div class="col-12">
                        <label class="form-label">Email</label>
                        <input type="email" name="userEmail" class="form-control form-control-lg"
                               value="<%= userEmail || '' %>" placeholder="name@example.com" required>
                    </div>
                    <div class="col-12">
                        <label class="form-label">Password</label>
                        <input type="password" name="userPassword" class="form-control form-control-lg" required>
                    </div>
                    <div class="col-12 d-flex justify-content-end">
                        <label class="form-label text-right"><a href="/forgotpassword" class="text-dark">Forgot Password</a></label>
                    </div>
                    <div class="col-12 pt-2">
                        <button class="btn btn-primary btn-lg w-100">Log In</button>
                    </div>
                    <div class="col-12 text-center">
                        <p class="mb-0">Don't have an account? <a href="/signUp" class="fw-semibold">Register</a></p>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
        crossorigin="anonymous"></script>
</body>
</html>